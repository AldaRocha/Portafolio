@{
                        //Esta nos indica cual vista iniciar, es decir este abre el html main del archivo 
    Layout = "_Layout"; //leyout. El controller manda a llamar un leyout que es el main y el render un body
                        //que se abrira depende de que html esta seleccionado en la ruta en "Program.cs"
} 
